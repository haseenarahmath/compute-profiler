# compute-profiler
A lightweight PyTorch-based toolkit to profile deep neural networks — computing FLOPs, parameter counts, and model complexity across MLP, CNN, and GNN architectures. Modular, extensible, and production-ready.
